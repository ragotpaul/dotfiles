#!/bin/bash

# General aliases
alias ls='ls --color=auto'
alias ll='ls -lh --color=auto'
alias la='ls -lah --color=auto'
alias grep='grep --color=auto'

# Git aliases
alias gll='git log --oneline --graph --decorate --all'
alias gst='git status -unormal --ignored'
